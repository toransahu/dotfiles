#!/usr/bin/env bash


DIR=$ME/dotfiles

self() {
    if [ -f $DIR/common/.self_rc ]; then
        source $DIR/common/.self_rc
    fi
}

mac() {
    if [ -f $DIR/macos/.mac_rc ]; then
        source $DIR/macos/.mac_rc
    fi
}

main(){
    self
    mac
}

main
