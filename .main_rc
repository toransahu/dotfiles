#!/usr/bin/env bash


DIR=$ME/aliases

self() {
    if [ -f $DIR/.self_rc.sh ]; then
        source $DIR/.self_rc
    fi
}

mac() {
    if [ -f $DIR/.mac_rc.sh ]; then
        source $DIR/.mac_rc
    fi
}


